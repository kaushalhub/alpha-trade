const Header = ({ darkMode, toggleDarkMode }) => (
  <div className="d-flex justify-content-between align-items-center mb-4">
    <h1 className="display-6">🔱 Alpha Trader</h1>
  </div>
);

export default Header;
